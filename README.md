# ansible-freeipa-nfs
This project uses Ansible to deploy a FreeIPA identity management environment consisting of one FreeIPA server and three FreeIPA client machines that are used to configure an NFS server so that user home directories are centrally stored and automatically mounted on every client.
